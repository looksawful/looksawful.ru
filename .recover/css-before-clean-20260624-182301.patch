diff --git a/src/styles/heading.css b/src/styles/heading.css
index 2b3b344..5bb3cde 100644
--- a/src/styles/heading.css
+++ b/src/styles/heading.css
@@ -27,3 +27,40 @@
 .text-block > p {
   max-width: var(--text);
 }
+
+@media (max-width: 48rem) {
+  .section-head,
+  .block__header,
+  .text-block {
+    width: 100%;
+    max-width: 100%;
+    gap: clamp(0.7rem, 3.5vw, 1rem);
+  }
+
+  .section-head > .title--xl,
+  .block__header > .title--xl {
+    justify-self: center;
+    max-width: min(100%, 14ch);
+    text-align: center;
+  }
+
+  .section-head > .title--lg,
+  .block__header > .title--lg,
+  .text-block > .title--lg,
+  .text-block > h1,
+  .text-block > h2,
+  .text-block > h3,
+  .text-block > h4,
+  .text-block > h5,
+  .text-block > h6 {
+    justify-self: start;
+    max-width: 100%;
+    text-align: left;
+  }
+
+  .section-head > p,
+  .block__header > p,
+  .text-block > p {
+    max-width: var(--text);
+  }
+}
diff --git a/src/styles/hero.css b/src/styles/hero.css
index 54b2d99..46ab498 100644
--- a/src/styles/hero.css
+++ b/src/styles/hero.css
@@ -52,6 +52,7 @@
 .hero-title-word {
   display: block;
   width: fit-content;
+  max-width: 100%;
   overflow: visible;
   white-space: nowrap;
 }
@@ -101,7 +102,12 @@
 
 @media (max-width: 48rem) {
   .hero__screen {
-    align-content: center;
+    min-height: 100vh;
+    min-height: 100svh;
+    align-content: start;
+    row-gap: clamp(1.6rem, 7vw, 2.6rem);
+    padding-block-start: calc(6.6rem + env(safe-area-inset-top));
+    padding-block-end: clamp(3.2rem, 13vw, 4.8rem);
   }
 
   .hero__headline-wrap {
@@ -110,27 +116,49 @@
   }
 
   .hero__title {
-    font-size: clamp(2.3rem, 10.6vw, 4.4rem);
-    line-height: 0.94;
+    font-size: var(--hero-main);
+    line-height: 0.92;
+    letter-spacing: -0.055em;
   }
 
+  .hero__title-name,
   .hero__title-role {
-    font-size: clamp(1.7rem, 8.8vw, 3.2rem);
-    line-height: 0.96;
-    letter-spacing: -0.02em;
+    max-width: 100%;
+    overflow-wrap: anywhere;
+    word-break: normal;
+    hyphens: none;
+  }
+
+  .hero__title-role {
+    font-size: var(--hero-sub);
+    line-height: 0.98;
+    letter-spacing: -0.025em;
+  }
+
+  .hero-title-word {
+    max-width: 100%;
+    white-space: normal;
   }
 
   .hero__title-role .hero-title-word {
-    margin-inline: auto 0;
+    margin-inline: 0 auto;
   }
 
   .hero__note,
   .contact-links {
-    max-width: min(100%, 24rem);
+    justify-self: start;
+    width: min(100%, 30rem);
+    max-width: min(100%, 30rem);
+  }
+
+  .hero__note {
+    line-height: 1.12;
+    overflow-wrap: normal;
+    word-break: normal;
+    hyphens: none;
   }
 
   .contact-links {
-    justify-self: start;
     text-align: left;
   }
 
@@ -143,10 +171,10 @@
 
 @media (max-width: 23rem) {
   .hero__title {
-    font-size: clamp(2rem, 10.8vw, 2.7rem);
+    font-size: clamp(2.45rem, 13.8vw, 3.2rem);
   }
 
   .hero__title-role {
-    font-size: clamp(1.5rem, 8.6vw, 2.1rem);
+    font-size: clamp(1.45rem, 8.2vw, 2rem);
   }
 }
diff --git a/src/styles/index.css b/src/styles/index.css
index dd5859d..720aa81 100644
--- a/src/styles/index.css
+++ b/src/styles/index.css
@@ -30,158 +30,3 @@
 @import "./proximity.css";
 @import "./policy-book.css";
 @import "./code-inspector.css";
-@import "./proximity.css";
-/* mobile layout patch:start */
-
-@media (max-width: 48rem) {
-  html,
-  body {
-    width: 100%;
-    max-width: 100%;
-    overflow-x: hidden;
-  }
-
-  main,
-  .section,
-  .section__screen,
-  .hero__screen {
-    min-width: 0;
-    max-width: 100%;
-  }
-
-  .section__screen,
-  .hero__screen {
-    grid-template-columns: minmax(0, 1fr);
-    padding-inline: clamp(16px, 4.8vw, 24px);
-  }
-
-  .section__screen > *,
-  .hero__screen > * {
-    grid-column: 1;
-    min-width: 0;
-    max-width: 100%;
-  }
-
-  .site-header {
-    --site-header-page-x: clamp(10px, 3.6vw, 16px);
-    --site-header-page-x-right: clamp(10px, 3.6vw, 16px);
-    --site-header-chip-gap: 0.5rem;
-    --site-header-chip-font: clamp(0.82rem, 3.6vw, 0.96rem);
-
-    padding-block: 0.55rem;
-    background: linear-gradient(
-      to bottom,
-      rgba(255, 255, 255, 0.94),
-      rgba(255, 255, 255, 0.84),
-      rgba(255, 255, 255, 0)
-    );
-  }
-
-  .site-header__island {
-    inline-size: 100%;
-    max-inline-size: 100%;
-    min-width: 0;
-  }
-
-  .site-header__chips {
-    inline-size: auto;
-    max-inline-size: calc(100vw - 4.5rem);
-    overflow-x: auto;
-    overflow-y: visible;
-    padding-block: 0.2rem;
-    padding-inline-end: 0.25rem;
-    scrollbar-width: none;
-    -webkit-overflow-scrolling: touch;
-  }
-
-  .site-header__chips::-webkit-scrollbar {
-    display: none;
-  }
-
-  .site-header__chip-slot,
-  .site-header__chip {
-    flex: 0 0 auto;
-  }
-
-  .site-header__chip,
-  .site-header__menu-link,
-  .site-header__trigger {
-    min-block-size: 2.65rem;
-    padding: 0.68rem 1rem 0.72rem;
-  }
-
-  .hero__screen {
-    min-height: 100vh;
-    min-height: 100svh;
-    align-content: center;
-    row-gap: clamp(2rem, 8vw, 3.2rem);
-    padding-block-start: calc(5rem + env(safe-area-inset-top));
-    padding-block-end: clamp(3.5rem, 12vw, 5rem);
-  }
-
-  .hero__headline-wrap {
-    justify-self: stretch;
-    width: 100%;
-    min-width: 0;
-    max-width: 100%;
-  }
-
-  .hero__title {
-    width: 100%;
-    min-width: 0;
-    max-width: 100%;
-    font-size: clamp(2.7rem, 15.5vw, 5.6rem);
-    line-height: 0.9;
-  }
-
-  .hero__title-name,
-  .hero__title-role,
-  .hero-title-word {
-    max-width: 100%;
-    min-width: 0;
-  }
-
-  .hero-title-word {
-    white-space: normal;
-    word-break: normal;
-    overflow-wrap: normal;
-    hyphens: none;
-  }
-
-  .hero__note {
-    justify-self: start;
-    width: min(100%, 25rem);
-    max-width: min(100%, 25rem);
-    min-width: 0;
-    font-size: clamp(1.1rem, 5vw, 1.45rem);
-    line-height: 1.08;
-    word-break: normal;
-    overflow-wrap: normal;
-    hyphens: none;
-  }
-
-  .contact-links {
-    justify-self: start;
-    width: min(100%, 25rem);
-    max-width: min(100%, 25rem);
-    text-align: left;
-  }
-
-  .contact-links a {
-    justify-self: start;
-  }
-
-  .section-head,
-  .block__header,
-  .text-block {
-    max-width: 100%;
-  }
-
-  .section-head > p,
-  .block__header > p,
-  .text-block > p {
-    max-width: min(100%, 34rem);
-  }
-}
-
-/* mobile layout patch:end */
diff --git a/src/styles/layout.css b/src/styles/layout.css
index 73338dd..22f5f1d 100644
--- a/src/styles/layout.css
+++ b/src/styles/layout.css
@@ -15,6 +15,7 @@
 .section__screen > *,
 .hero__screen > * {
   grid-column: 2;
+  min-width: 0;
 }
 
 .stack,
@@ -45,20 +46,17 @@
   .section__screen,
   .hero__screen {
     grid-template-columns: minmax(0, 1fr);
-    row-gap: clamp(2.1rem, 10vw, 3.6rem);
-    padding-block: clamp(3.4rem, 14vw, 5.4rem);
-    padding-inline: 10px;
+    row-gap: var(--section-gap);
+    padding-block: var(--section-gap);
+    padding-inline: var(--page-x);
   }
 
   .section__screen > *,
   .hero__screen > * {
     grid-column: 1;
   }
-}
 
-@media (max-width: 23rem) {
-  .section__screen,
-  .hero__screen {
-    padding-inline: 8px;
+  .showcase-stack {
+    gap: var(--project-gap);
   }
 }
diff --git a/src/styles/lead-text.css b/src/styles/lead-text.css
index 63434c1..ab4fd2d 100644
--- a/src/styles/lead-text.css
+++ b/src/styles/lead-text.css
@@ -14,8 +14,10 @@
   letter-spacing: -0.06em;
   text-transform: lowercase;
   text-wrap: balance;
+  overflow-wrap: anywhere;
 }
 
+
 .lead-center__text[data-words="short"] {
   font-size: clamp(4rem, 18vw, 15rem);
 }
@@ -93,6 +95,7 @@
   letter-spacing: -0.09em;
   text-transform: lowercase;
   text-wrap: pretty;
+  overflow-wrap: anywhere;
 }
 
 .lead-display--short p {
@@ -146,4 +149,4 @@
   .lead-display--multi p {
     font-size: clamp(2rem, 8vw, 4rem);
   }
-}
+}
\ No newline at end of file
diff --git a/src/styles/lists.css b/src/styles/lists.css
index 3263e24..c0957a5 100644
--- a/src/styles/lists.css
+++ b/src/styles/lists.css
@@ -169,6 +169,10 @@
 }
 
 @media (max-width: 48rem) {
+  .list-cards {
+    max-width: 100%;
+  }
+
   .list-cards--three {
     grid-template-columns: minmax(0, 1fr);
   }
@@ -181,6 +185,16 @@
   .list-cards--scroll {
     grid-auto-columns: minmax(min(82vw, 21.5rem), 1fr);
   }
+
+  .list-card {
+    border-radius: var(--r-lg);
+    background: var(--white);
+    box-shadow: var(--surface-elevation-1);
+  }
+
+  .list-card__title {
+    max-width: 100%;
+  }
 }
 
 /* Color token lists */
@@ -301,4 +315,4 @@
   .media-row--tokens > .media-slider {
     order: -1;
   }
-}
+}
\ No newline at end of file
diff --git a/src/styles/main.css b/src/styles/main.css
index 2fa804d..4234027 100644
--- a/src/styles/main.css
+++ b/src/styles/main.css
@@ -8,7 +8,8 @@
   --soft: rgba(0, 0, 0, 0.04);
   --r: 0.75rem;
   --r-lg: 1.25rem;
-  --page: min(100% - clamp(20px, 6vw, 96px), 1280px);
+  --page-x: clamp(20px, 6vw, 96px);
+  --page: min(calc(100% - (var(--page-x) * 2)), 1280px);
   --gap: clamp(1.1rem, 2.2vw, 2.2rem);
   --gap-lg: clamp(2rem, 4vw, 4.4rem);
   --section-gap: clamp(2.6rem, 5.5vw, 5.4rem);
@@ -24,6 +25,8 @@
   --hero-main: clamp(3rem, 10vw, 9rem);
   --hero-sub: clamp(3rem, 9vw, 8rem);
   --hero-text: clamp(1.05rem, 1.6vw, 1.25rem);
+  --surface-elevation-1: 0 0.35rem 1.4rem rgba(0, 0, 0, 0.08);
+  --surface-elevation-2: 0 0.7rem 2.4rem rgba(0, 0, 0, 0.12);
   --motion-fast: 160ms;
   --ease-standard: cubic-bezier(0.2, 0, 0, 1);
 }
@@ -43,6 +46,7 @@ html {
 }
 
 body {
+  min-width: 0;
   margin: 0;
   background: var(--white);
   color: var(--black);
@@ -54,7 +58,6 @@ canvas,
 svg {
   display: block;
   max-width: 100%;
-  max-width: 100%;
 }
 
 a {
@@ -86,3 +89,40 @@ ul {
 [hidden] {
   display: none !important;
 }
+
+@media (max-width: 48rem) {
+  :root {
+    --page-x: clamp(18px, 5vw, 24px);
+    --gap: clamp(0.9rem, 4vw, 1.35rem);
+    --gap-lg: clamp(1.55rem, 7vw, 2.75rem);
+    --section-gap: clamp(3.3rem, 13vw, 5.2rem);
+    --project-gap: clamp(2.4rem, 10vw, 4rem);
+    --component-gap: clamp(1.1rem, 5vw, 1.8rem);
+    --text: min(100%, 34rem);
+    --heading: 100%;
+    --xs: clamp(0.72rem, 3.1vw, 0.82rem);
+    --sm: clamp(0.84rem, 3.7vw, 0.96rem);
+    --md: clamp(0.98rem, 4.4vw, 1.15rem);
+    --lg: clamp(1.65rem, 7.6vw, 2.55rem);
+    --xl: clamp(2.25rem, 10.8vw, 4rem);
+    --hero-main: clamp(2.9rem, 14.5vw, 5.25rem);
+    --hero-sub: clamp(1.65rem, 8.4vw, 2.8rem);
+    --hero-text: clamp(1.05rem, 4.8vw, 1.34rem);
+  }
+
+  html,
+  body {
+    max-width: 100%;
+    overflow-x: hidden;
+  }
+
+  body {
+    overflow-x: clip;
+  }
+
+  @supports not (overflow-x: clip) {
+    body {
+      overflow-x: hidden;
+    }
+  }
+}
diff --git a/src/styles/media-grid.css b/src/styles/media-grid.css
index daac9d5..b2d152d 100644
--- a/src/styles/media-grid.css
+++ b/src/styles/media-grid.css
@@ -694,3 +694,18 @@
   height: 100%;
   object-fit: cover;
 }
+
+@media (max-width: 48rem) {
+  .media-overlap-three {
+    display: grid;
+    gap: var(--gap);
+    min-height: 0;
+    overflow: visible;
+  }
+
+  .media-overlap-three > .media-item {
+    position: static;
+    width: 100%;
+    transform: none;
+  }
+}
diff --git a/src/styles/project-responsibilities.css b/src/styles/project-responsibilities.css
index 279548e..c0e049c 100644
--- a/src/styles/project-responsibilities.css
+++ b/src/styles/project-responsibilities.css
@@ -205,14 +205,22 @@
 }
 
 @media (max-width: 48rem) {
+  .project-responsibilities,
+  .project-responsibilities--two,
+  .project-responsibilities--three {
+    grid-template-columns: minmax(0, 1fr);
+    gap: var(--gap);
+  }
+
   .project-responsibilities--mobile-scroll.project-responsibilities--three {
     display: grid;
     grid-auto-flow: column;
-    grid-auto-columns: minmax(17rem, min(78vw, 24rem));
+    grid-auto-columns: minmax(min(78vw, 17rem), min(82vw, 23rem));
     grid-template-columns: none;
     width: 100%;
     max-width: 100%;
     overflow-x: auto;
+    overflow-y: visible;
     overscroll-behavior-inline: contain;
     padding-bottom: 0.25rem;
     scroll-snap-type: x mandatory;
@@ -226,23 +234,40 @@
 
   .project-responsibilities--mobile-scroll.project-responsibilities--three > .responsibility-card {
     width: 100%;
-    min-width: 17rem;
-    max-width: 24rem;
+    min-width: 0;
+    max-width: none;
     grid-column: auto;
     scroll-snap-align: start;
   }
-}
 
-@media (max-width: 48rem) {
-  .project-responsibilities,
-  .project-responsibilities--two,
-  .project-responsibilities--three {
-    grid-template-columns: minmax(0, 1fr);
+  .project-responsibilities--contained-lists {
+    --responsibility-card-min-height: auto;
+    --responsibility-list-max-height: none;
+    --responsibility-list-bottom-space: 0;
   }
 
-  .project-responsibilities--contained-lists {
-    --responsibility-card-min-height: min(34rem, 78vh);
-    --responsibility-list-max-height: min(26rem, 58vh);
-    --responsibility-list-bottom-space: 4rem;
+  .project-responsibilities--contained-lists .responsibility-card {
+    grid-template-rows: auto;
+    min-height: 0;
+  }
+
+  .project-responsibilities--contained-lists .responsibility-card__list {
+    max-height: none;
+    padding-right: 0;
+    padding-bottom: 0;
+    overflow: visible;
+    scrollbar-gutter: auto;
+  }
+
+  .responsibility-card {
+    padding: clamp(1rem, 5vw, 1.35rem);
+    border-radius: var(--r-lg);
+    background: var(--white);
+    box-shadow: var(--surface-elevation-1);
+  }
+
+  .responsibility-card__title,
+  .project-responsibilities--semantic .responsibility-card__title {
+    font-size: var(--md);
   }
 }
diff --git a/src/styles/project.css b/src/styles/project.css
index 903a048..f724fa1 100644
--- a/src/styles/project.css
+++ b/src/styles/project.css
@@ -124,8 +124,66 @@
 }
 
 @media (max-width: 48rem) {
+  .project {
+    gap: var(--project-gap);
+    padding-block: var(--project-gap);
+  }
+
+  .project__header {
+    gap: var(--component-gap);
+  }
+
   .project__head,
   .project__head--text-only {
-    grid-template-columns: 1fr;
+    grid-template-columns: minmax(0, 1fr);
+    gap: var(--component-gap);
+  }
+
+  .project__head > .meta {
+    justify-self: center;
+    text-align: center;
+  }
+
+  .project__intro,
+  .project__head > div {
+    width: 100%;
+    max-width: 100%;
+    justify-items: center;
+  }
+
+  .project__intro > .title,
+  .project__head > div > .title {
+    max-width: min(100%, 13ch);
+    text-align: center;
+  }
+
+  .project__intro > p,
+  .project__head > div > p {
+    justify-self: start;
+    max-width: var(--text);
+    text-align: left;
+  }
+
+  .project__logo {
+    justify-self: center;
+  }
+
+  .project__logo img,
+  .project__logo canvas {
+    width: min(42vw, 10rem);
+  }
+
+  .project-section,
+  .block {
+    gap: var(--component-gap);
+    padding-block: var(--project-gap);
+  }
+
+  .project-section .project-section {
+    padding-block: var(--component-gap);
+  }
+
+  .project-skill-cloud__list {
+    max-width: 100%;
   }
 }
diff --git a/src/styles/random-gallery.css b/src/styles/random-gallery.css
index 2a71f60..8cac572 100644
--- a/src/styles/random-gallery.css
+++ b/src/styles/random-gallery.css
@@ -140,7 +140,7 @@
 .random-gallery.is-enhanced.is-visible .random-gallery__item:nth-child(9) { transition-delay: 280ms; }
 .random-gallery.is-enhanced.is-visible .random-gallery__item:nth-child(n + 10) { transition-delay: 315ms; }
 
-@media (max-width: 760px) {
+@media (max-width: 48rem) {
   .random-gallery__bar {
     align-items: flex-start;
     flex-direction: column;
@@ -149,6 +149,11 @@
   .random-gallery__shuffle {
     width: 100%;
   }
+
+  .random-gallery__item {
+    border-radius: var(--r-lg);
+    box-shadow: var(--surface-elevation-1);
+  }
 }
 
 @media (prefers-reduced-motion: reduce) {
@@ -163,4 +168,4 @@
     opacity: 1;
     transform: none;
   }
-}
+}
\ No newline at end of file
diff --git a/src/styles/resume.css b/src/styles/resume.css
index c2b7b92..e3f2687 100644
--- a/src/styles/resume.css
+++ b/src/styles/resume.css
@@ -28,10 +28,26 @@
   display: grid;
   gap: 0.7rem;
   max-width: var(--heading);
+  min-width: 0;
 }
 
 @media (max-width: 48rem) {
+  .resume-head {
+    text-align: center;
+  }
+
+  .resume-head > .title {
+    justify-self: center;
+    max-width: min(100%, 12ch);
+  }
+
   .resume-item {
-    grid-template-columns: 1fr;
+    grid-template-columns: minmax(0, 1fr);
+    gap: 0.9rem;
+    padding-block: var(--gap-lg);
+  }
+
+  .resume-item__header {
+    max-width: 100%;
   }
 }
diff --git a/src/styles/showcase-toc.css b/src/styles/showcase-toc.css
index ae11354..1a6add7 100644
--- a/src/styles/showcase-toc.css
+++ b/src/styles/showcase-toc.css
@@ -72,12 +72,16 @@
 @media (max-width: 64rem) {
   .showcase-toc {
     top: auto;
-    right: clamp(10px, 3vw, 20px);
+    right: var(--page-x);
     bottom: max(10px, env(safe-area-inset-bottom));
-    left: clamp(10px, 3vw, 20px);
+    left: var(--page-x);
     display: block;
     transform: none;
     overflow: hidden;
+    border: var(--line);
+    border-radius: 999px;
+    background: rgba(255, 255, 255, 0.92);
+    box-shadow: var(--surface-elevation-2);
   }
 
   .showcase-toc__list {
@@ -85,7 +89,8 @@
     gap: 0.45rem;
     max-width: 100%;
     overflow-x: auto;
-    padding-block: 0.25rem;
+    overflow-y: hidden;
+    padding: 0.42rem;
     scrollbar-width: none;
     -webkit-overflow-scrolling: touch;
   }
@@ -99,23 +104,27 @@
   .showcase-toc:focus-within .showcase-toc__link {
     flex: 0 0 auto;
     width: auto;
-    max-width: min(12rem, 72vw);
-    min-height: 1.6rem;
-    padding: 0.3rem 0;
+    min-height: 2rem;
+    max-width: min(13rem, 72vw);
+    padding: 0.38rem 0.65rem 0.42rem;
+    border-radius: 999px;
+  }
+
+  .showcase-toc__link::before {
+    flex-basis: 0.72rem;
+    width: 0.72rem;
   }
 
   .showcase-toc__text {
-    width: 0;
-    max-width: 0;
+    width: auto;
+    max-width: min(11rem, 58vw);
     overflow: hidden;
     opacity: 1;
+    text-overflow: ellipsis;
   }
 
-  .showcase-toc__link.is-active .showcase-toc__text,
-  .showcase-toc__link:focus-visible .showcase-toc__text,
-  .showcase-toc:hover .showcase-toc__text,
-  .showcase-toc:focus-within .showcase-toc__text {
-    width: auto;
-    max-width: min(10rem, 58vw);
+  .showcase-toc__link.is-active {
+    background: var(--black);
+    color: var(--white);
   }
 }
diff --git a/src/styles/site-header.css b/src/styles/site-header.css
index 7cd6724..ce7f3e9 100644
--- a/src/styles/site-header.css
+++ b/src/styles/site-header.css
@@ -192,23 +192,39 @@
     --site-header-chip-gap: clamp(0.48rem, 2.4vw, 0.74rem);
     --site-header-chip-font: clamp(0.82rem, 3.4svw, 1.02rem);
 
-    justify-content: flex-end;
+    justify-content: stretch;
     padding-block-start: max(0.72rem, env(safe-area-inset-top));
     padding-block-end: 0.62rem;
+    padding-inline: 0;
   }
 
   .site-header__island {
-    justify-items: end;
-    inline-size: fit-content;
-    max-inline-size: calc(100svw - var(--site-header-page-x) - var(--site-header-page-x-right));
-    margin-inline-start: auto;
-    margin-inline-end: 0;
+    justify-items: stretch;
+    inline-size: 100%;
+    max-inline-size: 100%;
+    margin: 0;
     transform-origin: top right;
   }
 
   .site-header__chips {
-    justify-content: flex-end;
-    max-inline-size: calc(100svw - var(--site-header-page-x) - var(--site-header-page-x-right));
+    justify-self: stretch;
+    justify-content: flex-start;
+    inline-size: 100%;
+    max-inline-size: 100%;
+    min-inline-size: 0;
+    overflow-x: auto;
+    overflow-y: visible;
+    overscroll-behavior-inline: contain;
+    padding-inline-start: var(--site-header-page-x);
+    padding-inline-end: var(--site-header-page-x-right);
+    padding-block: 0.2rem;
+    scroll-padding-inline: var(--site-header-page-x);
+    scrollbar-width: none;
+    -webkit-overflow-scrolling: touch;
+  }
+
+  .site-header__chips::-webkit-scrollbar {
+    display: none;
   }
 
   .site-header__chip {
@@ -224,10 +240,15 @@
     inline-size: 4.12rem;
     block-size: 4.12rem;
     min-block-size: 4.12rem;
+    margin-inline-end: var(--site-header-page-x-right);
     padding: 0.36rem;
     touch-action: manipulation;
   }
 
+  .site-header__island.is-collapsed {
+    justify-items: end;
+  }
+
   .site-header__island.is-collapsed .site-header__trigger {
     opacity: 1;
     visibility: visible;
@@ -249,7 +270,7 @@
 
   .site-header__menu {
     inset-block-start: calc(100% + clamp(0.54rem, 2.8svw, 0.82rem));
-    inset-inline-end: 0;
+    inset-inline-end: var(--site-header-page-x-right);
     inset-inline-start: auto;
     justify-items: end;
     gap: clamp(0.5rem, 2.6svw, 0.74rem);
@@ -266,4 +287,4 @@
     text-overflow: ellipsis;
   }
 }
-/* component site-header:end */
+/* component site-header:end */
\ No newline at end of file
diff --git a/src/styles/typography.css b/src/styles/typography.css
index 80e716b..aa4a5dd 100644
--- a/src/styles/typography.css
+++ b/src/styles/typography.css
@@ -7,11 +7,15 @@ p {
 }
 
 .title {
+  max-width: 100%;
+  min-width: 0;
   font-weight: 700;
   line-height: 0.95;
   letter-spacing: -0.04em;
   text-transform: lowercase;
   text-wrap: balance;
+  overflow-wrap: anywhere;
+  word-break: normal;
 }
 
 .title--display,
@@ -32,9 +36,11 @@ p {
   display: flex;
   flex-wrap: wrap;
   gap: 0.35rem;
+  min-width: 0;
 }
 
 .chip {
+  min-width: 0;
   padding: 0.26rem 0.5rem 0.3rem;
   border: var(--line);
   border-radius: 999px;
@@ -51,11 +57,39 @@ p {
   display: grid;
   gap: 0.35rem;
   max-width: var(--text);
+  min-width: 0;
   padding: 0;
   list-style: none;
 }
 
 .text-list li {
+  min-width: 0;
   font-size: var(--md);
   line-height: 1.25;
 }
+
+@media (max-width: 48rem) {
+  p,
+  .text-list li {
+    line-height: 1.22;
+  }
+
+  .title--display,
+  .title--xl {
+    line-height: 0.96;
+    letter-spacing: -0.052em;
+  }
+
+  .title--lg {
+    line-height: 1;
+    letter-spacing: -0.042em;
+  }
+
+  .chips {
+    gap: 0.42rem;
+  }
+
+  .chip {
+    padding: 0.28rem 0.58rem 0.32rem;
+  }
+}
