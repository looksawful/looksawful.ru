diff --git a/index.html b/index.html
index e003433..4501aed 100644
--- a/index.html
+++ b/index.html
@@ -4681,3 +4681,5 @@
 
 </html>
 
+
+
diff --git a/src/components/media-slider-dots-proximity.js b/src/components/media-slider-dots-proximity.js
index e0d5dbc..a802cd4 100644
--- a/src/components/media-slider-dots-proximity.js
+++ b/src/components/media-slider-dots-proximity.js
@@ -1,10 +1,14 @@
-export { initMediaSliderDotsProximity } from "./components/proximity-components.js";
+export {
+  initMediaSliderDotsProximity
+} from "./proximity-components.js";
 
-import { initMediaSliderDotsProximity } from "./components/proximity-components.js";
+import {
+  initMediaSliderDotsProximity
+} from "./proximity-components.js";
 
 if (document.readyState === "loading") {
   document.addEventListener("DOMContentLoaded", initMediaSliderDotsProximity, {
-    once: true,
+    once: true
   });
 } else {
   initMediaSliderDotsProximity();
diff --git a/src/components/proximity-components.js b/src/components/proximity-components.js
index 5387b41..82af8e9 100644
--- a/src/components/proximity-components.js
+++ b/src/components/proximity-components.js
@@ -1,37 +1,66 @@
-import { createObservedInitializer, createProximityMotion } from "./proximity-core.js";
+import {
+  createObservedInitializer,
+  createProximityMotion
+} from "./proximity-core.js";
 
 const initializedDots = new WeakSet();
 const initializedArrows = new WeakSet();
 const initializedOpenControls = new WeakSet();
 const initializedFullscreenControls = new WeakSet();
+const initializedVideoControls = new WeakSet();
 const initializedCaptions = new WeakSet();
 const initializedToc = new WeakSet();
 const initializedLightboxClose = new WeakSet();
 const initializedCopyControls = new WeakSet();
 
-const fullscreenIcon = `
-  <svg viewBox="0 0 16 16" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
-    <path d="M2 6V2h4M10 2h4v4M14 10v4h-4M6 14H2v-4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"></path>
-  </svg>
-`;
-
-const openIcon = `
-  <svg viewBox="0 0 16 16" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
-    <path d="M6 3H3v10h10v-3M9 3h4v4M13 3 7 9" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"></path>
-  </svg>
-`;
-
-const copyIcon = `
-  <svg viewBox="0 0 16 16" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
-    <path d="M5 5V3h8v8h-2M3 5h8v8H3V5Z" stroke="currentColor" stroke-width="1.5" stroke-linejoin="round"></path>
-  </svg>
-`;
-
-const createProximityControl = ({ tag = "button", className = "", label, icon = fullscreenIcon }) => {
+const fullscreenIcon = [
+  '<svg viewBox="0 0 16 16" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">',
+  '<path d="M2 6V2h4M10 2h4v4M14 10v4h-4M6 14H2v-4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"></path>',
+  '</svg>'
+].join("");
+
+const lightboxIcon = [
+  '<svg viewBox="0 0 16 16" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">',
+  '<circle cx="7" cy="7" r="3.25" stroke="currentColor" stroke-width="1.5"></circle>',
+  '<path d="M9.5 9.5 13 13" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"></path>',
+  '</svg>'
+].join("");
+
+const externalIcon = [
+  '<svg viewBox="0 0 16 16" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">',
+  '<path d="M6 3H3v10h10v-3M9 3h4v4M13 3 7 9" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"></path>',
+  '</svg>'
+].join("");
+
+const playIcon = [
+  '<svg viewBox="0 0 16 16" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">',
+  '<path d="M5.2 3.6 12.2 8l-7 4.4V3.6Z" fill="currentColor"></path>',
+  '</svg>'
+].join("");
+
+const pauseIcon = [
+  '<svg viewBox="0 0 16 16" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">',
+  '<path d="M5.5 4v8M10.5 4v8" stroke="currentColor" stroke-width="1.7" stroke-linecap="round"></path>',
+  '</svg>'
+].join("");
+
+const copyIcon = [
+  '<svg viewBox="0 0 16 16" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">',
+  '<path d="M5 5V3h8v8h-2M3 5h8v8H3V5Z" stroke="currentColor" stroke-width="1.5" stroke-linejoin="round"></path>',
+  '</svg>'
+].join("");
+
+const createProximityControl = ({
+  tag = "button",
+  className = "",
+  label,
+  icon = fullscreenIcon
+}) => {
   const control = document.createElement(tag);
 
   control.className = `proximity-control ${className}`.trim();
   control.innerHTML = icon;
+  control.dataset.proximityControl = "true";
 
   if (label) {
     control.setAttribute("aria-label", label);
@@ -44,6 +73,21 @@ const createProximityControl = ({ tag = "button", className = "", label, icon =
   return control;
 };
 
+const isExternalHref = (element) => {
+  const href = element.getAttribute("href");
+
+  if (!href || href.startsWith("#") || href.startsWith("/") || href.startsWith("./") || href.startsWith("../")) {
+    return false;
+  }
+
+  try {
+    const url = new URL(href, window.location.href);
+    return url.origin !== window.location.origin;
+  } catch {
+    return false;
+  }
+};
+
 const requestElementFullscreen = (element) => {
   if (!element) {
     return;
@@ -57,6 +101,15 @@ const requestElementFullscreen = (element) => {
   element.requestFullscreen?.();
 };
 
+const getVideoContainer = (video) => {
+  return video.closest("[data-showcase-inline-video], .media-item, .media, .embedded-demo, [data-animation], [data-visual-demo]");
+};
+
+const updateVideoControlIcon = (control, video) => {
+  control.innerHTML = video.paused ? playIcon : pauseIcon;
+  control.setAttribute("aria-label", video.paused ? "play video" : "pause video");
+};
+
 export const initMediaSliderDotsProximity = () => {
   document.querySelectorAll(".media-slider").forEach((slider) => {
     const dots = slider.querySelector(".media-slider__dots");
@@ -80,7 +133,7 @@ export const initMediaSliderDotsProximity = () => {
       scaleYFrom: 0.94,
       scaleYTo: 1,
       transformOrigin: "bottom center",
-      activeClassTarget: dots,
+      activeClassTarget: dots
     });
   });
 };
@@ -111,7 +164,7 @@ export const initMediaSliderArrowProximity = () => {
         scaleYFrom: 1,
         scaleYTo: 1,
         transformOrigin: "center",
-        activeClassTarget: arrow,
+        activeClassTarget: arrow
       });
     });
   });
@@ -131,11 +184,19 @@ export const initMediaItemOpenProximity = () => {
     initializedOpenControls.add(item);
     item.classList.add("has-proximity-control");
 
+    const hasVideo = Boolean(item.querySelector("video")) || /\.(mp4|webm|mov)(\?.*)?(#.*)?$/i.test(item.getAttribute("href") || "");
+    const isExternal = isExternalHref(item);
+
     const control = createProximityControl({
       tag: "span",
-      label: "open media",
-      icon: openIcon,
-      className: "proximity-control--top-right proximity-control--open",
+      label: isExternal ? "open external link" : hasVideo ? "open video" : "open media",
+      icon: isExternal ? externalIcon : hasVideo ? playIcon : lightboxIcon,
+      className: [
+        "proximity-control--top-right",
+        "proximity-control--open",
+        isExternal ? "proximity-control--external" : "",
+        hasVideo ? "proximity-control--video-link" : "proximity-control--lightbox"
+      ].filter(Boolean).join(" ")
     });
 
     control.setAttribute("aria-hidden", "true");
@@ -154,19 +215,31 @@ export const initMediaItemOpenProximity = () => {
       scaleYFrom: 0.94,
       scaleYTo: 1,
       transformOrigin: "center",
-      activeClassTarget: control,
+      activeClassTarget: control
     });
   });
 };
 
 export const initMediaFullscreenProximity = () => {
-  const selector = [".media", ".embedded-demo", "[data-animation]", "[data-visual-demo]"].join(",");
+  const selector = [
+    ".media",
+    ".embedded-demo",
+    "[data-animation]",
+    "[data-visual-demo]",
+    "[data-showcase-inline-video]"
+  ].join(",");
 
   document.querySelectorAll(selector).forEach((element) => {
     if (initializedFullscreenControls.has(element)) {
       return;
     }
 
+    const mediaParent = element.closest(".media");
+
+    if (mediaParent && mediaParent !== element) {
+      return;
+    }
+
     if (element.closest(".media-slider")) {
       return;
     }
@@ -175,11 +248,15 @@ export const initMediaFullscreenProximity = () => {
       return;
     }
 
+    if (element.querySelector(":scope > a.media-item")) {
+      return;
+    }
+
     if (!element.querySelector("img, video, canvas")) {
       return;
     }
 
-    if (element.querySelector(".proximity-control--fullscreen")) {
+    if (element.querySelector(":scope > .proximity-control--fullscreen")) {
       initializedFullscreenControls.add(element);
       return;
     }
@@ -190,7 +267,7 @@ export const initMediaFullscreenProximity = () => {
     const control = createProximityControl({
       label: "fullscreen",
       icon: fullscreenIcon,
-      className: "proximity-control--top-right proximity-control--fullscreen",
+      className: "proximity-control--top-right proximity-control--fullscreen"
     });
 
     element.append(control);
@@ -216,10 +293,80 @@ export const initMediaFullscreenProximity = () => {
       scaleYFrom: 0.94,
       scaleYTo: 1,
       transformOrigin: "center",
-      activeClassTarget: control,
+      activeClassTarget: control
+    });
+  });
+};
+
+export const initMediaVideoProximity = () => {
+  document.querySelectorAll("video").forEach((video) => {
+    const container = getVideoContainer(video);
+
+    if (!container || initializedVideoControls.has(container)) {
+      return;
+    }
+
+    if (container.matches("a.media-item[href]")) {
+      initializedVideoControls.add(container);
+      return;
+    }
+
+    if (container.querySelector(":scope > .proximity-control--video")) {
+      initializedVideoControls.add(container);
+      return;
+    }
+
+    initializedVideoControls.add(container);
+    container.classList.add("has-proximity-control");
+
+    const control = createProximityControl({
+      label: "play video",
+      icon: playIcon,
+      className: "proximity-control--bottom-left proximity-control--video"
+    });
+
+    updateVideoControlIcon(control, video);
+    container.append(control);
+
+    control.addEventListener("click", async (event) => {
+      event.preventDefault();
+      event.stopPropagation();
+
+      if (video.paused) {
+        try {
+          await video.play();
+        } catch {
+          return;
+        }
+      } else {
+        video.pause();
+      }
+
+      updateVideoControlIcon(control, video);
+    });
+
+    video.addEventListener("play", () => updateVideoControlIcon(control, video));
+    video.addEventListener("pause", () => updateVideoControlIcon(control, video));
+    video.addEventListener("ended", () => updateVideoControlIcon(control, video));
+
+    createProximityMotion({
+      root: container,
+      target: control,
+      distanceTarget: control,
+      showStart: 220,
+      showEnd: 22,
+      yFrom: 8,
+      yTo: 0,
+      scaleXFrom: 0.94,
+      scaleXTo: 1,
+      scaleYFrom: 0.94,
+      scaleYTo: 1,
+      transformOrigin: "center",
+      activeClassTarget: control
     });
   });
 };
+
 export const initMediaCaptionProximity = () => {
   document.querySelectorAll(".media-figure").forEach((figure) => {
     const caption = figure.querySelector(":scope > .media-caption");
@@ -244,7 +391,7 @@ export const initMediaCaptionProximity = () => {
       scaleYFrom: 1,
       scaleYTo: 1,
       transformOrigin: "top left",
-      activeClassTarget: caption,
+      activeClassTarget: caption
     });
   });
 };
@@ -259,6 +406,7 @@ export const initShowcaseTocProximity = () => {
 
     createProximityMotion({
       root: document,
+      listenRoot: document,
       target: toc,
       distanceTarget: toc,
       showStart: 220,
@@ -277,7 +425,7 @@ export const initShowcaseTocProximity = () => {
 
         toc.style.setProperty("--showcase-toc-link-width", `${linkWidth}rem`);
         toc.style.setProperty("--showcase-toc-text-opacity", textOpacity.toFixed(3));
-      },
+      }
     });
   });
 };
@@ -306,7 +454,7 @@ export const initLightboxCloseProximity = () => {
       scaleYFrom: 0.94,
       scaleYTo: 1,
       transformOrigin: "center",
-      activeClassTarget: close,
+      activeClassTarget: close
     });
   });
 };
@@ -322,7 +470,11 @@ const getCopyText = (block) => {
 };
 
 export const initCodeCopyProximity = () => {
-  const selector = ["pre", ".code-block", ".code-inspector"].join(",");
+  const selector = [
+    "pre",
+    ".code-block",
+    ".code-inspector"
+  ].join(",");
 
   document.querySelectorAll(selector).forEach((block) => {
     if (initializedCopyControls.has(block)) {
@@ -340,7 +492,7 @@ export const initCodeCopyProximity = () => {
     const control = createProximityControl({
       label: "copy code",
       icon: copyIcon,
-      className: "proximity-control--top-right proximity-control--copy",
+      className: "proximity-control--top-right proximity-control--copy"
     });
 
     block.append(control);
@@ -376,7 +528,7 @@ export const initCodeCopyProximity = () => {
       scaleYFrom: 0.94,
       scaleYTo: 1,
       transformOrigin: "center",
-      activeClassTarget: control,
+      activeClassTarget: control
     });
   });
 };
@@ -386,6 +538,7 @@ export const initProximityComponents = () => {
   initMediaSliderArrowProximity();
   initMediaItemOpenProximity();
   initMediaFullscreenProximity();
+  initMediaVideoProximity();
   initMediaCaptionProximity();
   initShowcaseTocProximity();
   initLightboxCloseProximity();
diff --git a/src/components/proximity-core.js b/src/components/proximity-core.js
index 83f91b7..96d50c5 100644
--- a/src/components/proximity-core.js
+++ b/src/components/proximity-core.js
@@ -14,7 +14,11 @@ export const getDistanceToElement = (event, element) => {
   return Math.hypot(distanceX, distanceY);
 };
 
-export const getProximityProgress = ({ distance, showStart = 220, showEnd = 24 }) => {
+export const getProximityProgress = ({
+  distance,
+  showStart = 220,
+  showEnd = 24
+}) => {
   if (showStart === showEnd) {
     return distance <= showEnd ? 1 : 0;
   }
@@ -55,7 +59,7 @@ export const createProximityMotion = ({
   hideOnRootLeave = listenRoot !== document,
 
   onProgress = null,
-  onReady = null,
+  onReady = null
 }) => {
   if (!root || !target || !distanceTarget || !listenRoot) {
     return null;
@@ -75,7 +79,7 @@ export const createProximityMotion = ({
       y,
       scaleX,
       scaleY,
-      transformOrigin,
+      transformOrigin
     });
 
     if (activeClassTarget?.classList) {
@@ -96,14 +100,14 @@ export const createProximityMotion = ({
   };
 
   if (!canUsePointerProximity()) {
-    show();
+    hide();
 
     if (onReady) {
       onReady();
     }
 
     return {
-      destroy: () => {},
+      destroy: () => {}
     };
   }
 
@@ -114,12 +118,24 @@ export const createProximityMotion = ({
     const progress = getProximityProgress({
       distance,
       showStart,
-      showEnd,
+      showEnd
     });
 
     setProgress(progress);
   };
 
+  const hideOnViewportLeave = (event) => {
+    if (!event.relatedTarget) {
+      hide();
+    }
+  };
+
+  const hideOnVisibilityChange = () => {
+    if (document.hidden) {
+      hide();
+    }
+  };
+
   listenRoot.addEventListener("pointermove", update);
 
   if (hideOnRootLeave) {
@@ -127,7 +143,9 @@ export const createProximityMotion = ({
   }
 
   window.addEventListener("blur", hide);
-  document.addEventListener("visibilitychange", hide);
+  window.addEventListener("pointerout", hideOnViewportLeave);
+  window.addEventListener("scroll", hide, { passive: true });
+  document.addEventListener("visibilitychange", hideOnVisibilityChange);
 
   target.addEventListener("focusin", show);
   target.addEventListener("focusout", hide);
@@ -145,11 +163,13 @@ export const createProximityMotion = ({
       }
 
       window.removeEventListener("blur", hide);
-      document.removeEventListener("visibilitychange", hide);
+      window.removeEventListener("pointerout", hideOnViewportLeave);
+      window.removeEventListener("scroll", hide);
+      document.removeEventListener("visibilitychange", hideOnVisibilityChange);
 
       target.removeEventListener("focusin", show);
       target.removeEventListener("focusout", hide);
-    },
+    }
   };
 };
 
@@ -169,26 +189,26 @@ export const createObservedInitializer = (init) => {
 
     observer.observe(document.documentElement, {
       childList: true,
-      subtree: true,
+      subtree: true
     });
 
     return {
       destroy: () => {
         observer?.disconnect();
         observer = null;
-      },
+      }
     };
   };
 
   if (document.readyState === "loading") {
     document.addEventListener("DOMContentLoaded", start, {
-      once: true,
+      once: true
     });
 
     return {
       destroy: () => {
         document.removeEventListener("DOMContentLoaded", start);
-      },
+      }
     };
   }
 
diff --git a/src/styles/heading.css b/src/styles/heading.css
index 2b3b344..e46d437 100644
--- a/src/styles/heading.css
+++ b/src/styles/heading.css
@@ -27,3 +27,42 @@
 .text-block > p {
   max-width: var(--text);
 }
+
+@media (max-width: 48rem) {
+  .section-head,
+  .block__header,
+  .text-block {
+    width: 100%;
+    max-width: 100%;
+    gap: clamp(0.7rem, 3.5vw, 1rem);
+  }
+
+  .section-head > .title--xl,
+  .block__header > .title--xl {
+    justify-self: center;
+    max-width: min(100%, 14ch);
+    text-align: center;
+  }
+
+  .section-head > .title--lg,
+  .block__header > .title--lg,
+  .text-block > .title--lg,
+  .text-block > h1,
+  .text-block > h2,
+  .text-block > h3,
+  .text-block > h4,
+  .text-block > h5,
+  .text-block > h6 {
+    justify-self: start;
+    max-width: 100%;
+    text-align: left;
+  }
+
+  .section-head > p,
+  .block__header > p,
+  .text-block > p {
+    max-width: var(--text);
+  }
+}
+
+
diff --git a/src/styles/hero.css b/src/styles/hero.css
index 54b2d99..8c28964 100644
--- a/src/styles/hero.css
+++ b/src/styles/hero.css
@@ -52,6 +52,7 @@
 .hero-title-word {
   display: block;
   width: fit-content;
+  max-width: 100%;
   overflow: visible;
   white-space: nowrap;
 }
@@ -101,7 +102,12 @@
 
 @media (max-width: 48rem) {
   .hero__screen {
-    align-content: center;
+    min-height: 100vh;
+    min-height: 100svh;
+    align-content: start;
+    row-gap: clamp(1.6rem, 7vw, 2.6rem);
+    padding-block-start: calc(6.6rem + env(safe-area-inset-top));
+    padding-block-end: clamp(3.2rem, 13vw, 4.8rem);
   }
 
   .hero__headline-wrap {
@@ -110,27 +116,49 @@
   }
 
   .hero__title {
-    font-size: clamp(2.3rem, 10.6vw, 4.4rem);
-    line-height: 0.94;
+    font-size: var(--hero-main);
+    line-height: 0.92;
+    letter-spacing: -0.055em;
   }
 
+  .hero__title-name,
   .hero__title-role {
-    font-size: clamp(1.7rem, 8.8vw, 3.2rem);
-    line-height: 0.96;
-    letter-spacing: -0.02em;
+    max-width: 100%;
+    overflow-wrap: anywhere;
+    word-break: normal;
+    hyphens: none;
+  }
+
+  .hero__title-role {
+    font-size: var(--hero-sub);
+    line-height: 0.98;
+    letter-spacing: -0.025em;
+  }
+
+  .hero-title-word {
+    max-width: 100%;
+    white-space: normal;
   }
 
   .hero__title-role .hero-title-word {
-    margin-inline: auto 0;
+    margin-inline: 0 auto;
   }
 
   .hero__note,
   .contact-links {
-    max-width: min(100%, 24rem);
+    justify-self: start;
+    width: min(100%, 30rem);
+    max-width: min(100%, 30rem);
+  }
+
+  .hero__note {
+    line-height: 1.12;
+    overflow-wrap: normal;
+    word-break: normal;
+    hyphens: none;
   }
 
   .contact-links {
-    justify-self: start;
     text-align: left;
   }
 
@@ -143,10 +171,12 @@
 
 @media (max-width: 23rem) {
   .hero__title {
-    font-size: clamp(2rem, 10.8vw, 2.7rem);
+    font-size: clamp(2.45rem, 13.8vw, 3.2rem);
   }
 
   .hero__title-role {
-    font-size: clamp(1.5rem, 8.6vw, 2.1rem);
+    font-size: clamp(1.45rem, 8.2vw, 2rem);
   }
 }
+
+
diff --git a/src/styles/index.css b/src/styles/index.css
index dd5859d..44cc8df 100644
--- a/src/styles/index.css
+++ b/src/styles/index.css
@@ -30,158 +30,6 @@
 @import "./proximity.css";
 @import "./policy-book.css";
 @import "./code-inspector.css";
-@import "./proximity.css";
-/* mobile layout patch:start */
-
-@media (max-width: 48rem) {
-  html,
-  body {
-    width: 100%;
-    max-width: 100%;
-    overflow-x: hidden;
-  }
-
-  main,
-  .section,
-  .section__screen,
-  .hero__screen {
-    min-width: 0;
-    max-width: 100%;
-  }
-
-  .section__screen,
-  .hero__screen {
-    grid-template-columns: minmax(0, 1fr);
-    padding-inline: clamp(16px, 4.8vw, 24px);
-  }
-
-  .section__screen > *,
-  .hero__screen > * {
-    grid-column: 1;
-    min-width: 0;
-    max-width: 100%;
-  }
-
-  .site-header {
-    --site-header-page-x: clamp(10px, 3.6vw, 16px);
-    --site-header-page-x-right: clamp(10px, 3.6vw, 16px);
-    --site-header-chip-gap: 0.5rem;
-    --site-header-chip-font: clamp(0.82rem, 3.6vw, 0.96rem);
-
-    padding-block: 0.55rem;
-    background: linear-gradient(
-      to bottom,
-      rgba(255, 255, 255, 0.94),
-      rgba(255, 255, 255, 0.84),
-      rgba(255, 255, 255, 0)
-    );
-  }
-
-  .site-header__island {
-    inline-size: 100%;
-    max-inline-size: 100%;
-    min-width: 0;
-  }
-
-  .site-header__chips {
-    inline-size: auto;
-    max-inline-size: calc(100vw - 4.5rem);
-    overflow-x: auto;
-    overflow-y: visible;
-    padding-block: 0.2rem;
-    padding-inline-end: 0.25rem;
-    scrollbar-width: none;
-    -webkit-overflow-scrolling: touch;
-  }
-
-  .site-header__chips::-webkit-scrollbar {
-    display: none;
-  }
-
-  .site-header__chip-slot,
-  .site-header__chip {
-    flex: 0 0 auto;
-  }
-
-  .site-header__chip,
-  .site-header__menu-link,
-  .site-header__trigger {
-    min-block-size: 2.65rem;
-    padding: 0.68rem 1rem 0.72rem;
-  }
-
-  .hero__screen {
-    min-height: 100vh;
-    min-height: 100svh;
-    align-content: center;
-    row-gap: clamp(2rem, 8vw, 3.2rem);
-    padding-block-start: calc(5rem + env(safe-area-inset-top));
-    padding-block-end: clamp(3.5rem, 12vw, 5rem);
-  }
-
-  .hero__headline-wrap {
-    justify-self: stretch;
-    width: 100%;
-    min-width: 0;
-    max-width: 100%;
-  }
-
-  .hero__title {
-    width: 100%;
-    min-width: 0;
-    max-width: 100%;
-    font-size: clamp(2.7rem, 15.5vw, 5.6rem);
-    line-height: 0.9;
-  }
-
-  .hero__title-name,
-  .hero__title-role,
-  .hero-title-word {
-    max-width: 100%;
-    min-width: 0;
-  }
-
-  .hero-title-word {
-    white-space: normal;
-    word-break: normal;
-    overflow-wrap: normal;
-    hyphens: none;
-  }
-
-  .hero__note {
-    justify-self: start;
-    width: min(100%, 25rem);
-    max-width: min(100%, 25rem);
-    min-width: 0;
-    font-size: clamp(1.1rem, 5vw, 1.45rem);
-    line-height: 1.08;
-    word-break: normal;
-    overflow-wrap: normal;
-    hyphens: none;
-  }
-
-  .contact-links {
-    justify-self: start;
-    width: min(100%, 25rem);
-    max-width: min(100%, 25rem);
-    text-align: left;
-  }
-
-  .contact-links a {
-    justify-self: start;
-  }
 
-  .section-head,
-  .block__header,
-  .text-block {
-    max-width: 100%;
-  }
 
-  .section-head > p,
-  .block__header > p,
-  .text-block > p {
-    max-width: min(100%, 34rem);
-  }
-}
 
-/* mobile layout patch:end */
diff --git a/src/styles/layout.css b/src/styles/layout.css
index 73338dd..d3af2fe 100644
--- a/src/styles/layout.css
+++ b/src/styles/layout.css
@@ -15,6 +15,7 @@
 .section__screen > *,
 .hero__screen > * {
   grid-column: 2;
+  min-width: 0;
 }
 
 .stack,
@@ -45,20 +46,19 @@
   .section__screen,
   .hero__screen {
     grid-template-columns: minmax(0, 1fr);
-    row-gap: clamp(2.1rem, 10vw, 3.6rem);
-    padding-block: clamp(3.4rem, 14vw, 5.4rem);
-    padding-inline: 10px;
+    row-gap: var(--section-gap);
+    padding-block: var(--section-gap);
+    padding-inline: var(--page-x);
   }
 
   .section__screen > *,
   .hero__screen > * {
     grid-column: 1;
   }
-}
 
-@media (max-width: 23rem) {
-  .section__screen,
-  .hero__screen {
-    padding-inline: 8px;
+  .showcase-stack {
+    gap: var(--project-gap);
   }
 }
+
+
diff --git a/src/styles/lead-text.css b/src/styles/lead-text.css
index 63434c1..b97feff 100644
--- a/src/styles/lead-text.css
+++ b/src/styles/lead-text.css
@@ -14,8 +14,10 @@
   letter-spacing: -0.06em;
   text-transform: lowercase;
   text-wrap: balance;
+  overflow-wrap: anywhere;
 }
 
+
 .lead-center__text[data-words="short"] {
   font-size: clamp(4rem, 18vw, 15rem);
 }
@@ -93,6 +95,7 @@
   letter-spacing: -0.09em;
   text-transform: lowercase;
   text-wrap: pretty;
+  overflow-wrap: anywhere;
 }
 
 .lead-display--short p {
@@ -147,3 +150,4 @@
     font-size: clamp(2rem, 8vw, 4rem);
   }
 }
+
diff --git a/src/styles/lists.css b/src/styles/lists.css
index 3263e24..af4dedb 100644
--- a/src/styles/lists.css
+++ b/src/styles/lists.css
@@ -169,6 +169,10 @@
 }
 
 @media (max-width: 48rem) {
+  .list-cards {
+    max-width: 100%;
+  }
+
   .list-cards--three {
     grid-template-columns: minmax(0, 1fr);
   }
@@ -181,6 +185,16 @@
   .list-cards--scroll {
     grid-auto-columns: minmax(min(82vw, 21.5rem), 1fr);
   }
+
+  .list-card {
+    border-radius: var(--r-lg);
+    background: var(--white);
+    box-shadow: var(--surface-elevation-1);
+  }
+
+  .list-card__title {
+    max-width: 100%;
+  }
 }
 
 /* Color token lists */
@@ -302,3 +316,4 @@
     order: -1;
   }
 }
+
diff --git a/src/styles/main.css b/src/styles/main.css
index 2fa804d..8405ac7 100644
--- a/src/styles/main.css
+++ b/src/styles/main.css
@@ -8,7 +8,8 @@
   --soft: rgba(0, 0, 0, 0.04);
   --r: 0.75rem;
   --r-lg: 1.25rem;
-  --page: min(100% - clamp(20px, 6vw, 96px), 1280px);
+  --page-x: clamp(20px, 6vw, 96px);
+  --page: min(calc(100% - (var(--page-x) * 2)), 1280px);
   --gap: clamp(1.1rem, 2.2vw, 2.2rem);
   --gap-lg: clamp(2rem, 4vw, 4.4rem);
   --section-gap: clamp(2.6rem, 5.5vw, 5.4rem);
@@ -24,6 +25,8 @@
   --hero-main: clamp(3rem, 10vw, 9rem);
   --hero-sub: clamp(3rem, 9vw, 8rem);
   --hero-text: clamp(1.05rem, 1.6vw, 1.25rem);
+  --surface-elevation-1: 0 0.35rem 1.4rem rgba(0, 0, 0, 0.08);
+  --surface-elevation-2: 0 0.7rem 2.4rem rgba(0, 0, 0, 0.12);
   --motion-fast: 160ms;
   --ease-standard: cubic-bezier(0.2, 0, 0, 1);
 }
@@ -43,6 +46,7 @@ html {
 }
 
 body {
+  min-width: 0;
   margin: 0;
   background: var(--white);
   color: var(--black);
@@ -54,7 +58,6 @@ canvas,
 svg {
   display: block;
   max-width: 100%;
-  max-width: 100%;
 }
 
 a {
@@ -86,3 +89,42 @@ ul {
 [hidden] {
   display: none !important;
 }
+
+@media (max-width: 48rem) {
+  :root {
+    --page-x: clamp(18px, 5vw, 24px);
+    --gap: clamp(0.9rem, 4vw, 1.35rem);
+    --gap-lg: clamp(1.55rem, 7vw, 2.75rem);
+    --section-gap: clamp(3.3rem, 13vw, 5.2rem);
+    --project-gap: clamp(2.4rem, 10vw, 4rem);
+    --component-gap: clamp(1.1rem, 5vw, 1.8rem);
+    --text: min(100%, 34rem);
+    --heading: 100%;
+    --xs: clamp(0.72rem, 3.1vw, 0.82rem);
+    --sm: clamp(0.84rem, 3.7vw, 0.96rem);
+    --md: clamp(0.98rem, 4.4vw, 1.15rem);
+    --lg: clamp(1.65rem, 7.6vw, 2.55rem);
+    --xl: clamp(2.25rem, 10.8vw, 4rem);
+    --hero-main: clamp(2.9rem, 14.5vw, 5.25rem);
+    --hero-sub: clamp(1.65rem, 8.4vw, 2.8rem);
+    --hero-text: clamp(1.05rem, 4.8vw, 1.34rem);
+  }
+
+  html,
+  body {
+    max-width: 100%;
+    overflow-x: hidden;
+  }
+
+  body {
+    overflow-x: clip;
+  }
+
+  @supports not (overflow-x: clip) {
+    body {
+      overflow-x: hidden;
+    }
+  }
+}
+
+
diff --git a/src/styles/media-grid.css b/src/styles/media-grid.css
index daac9d5..25d5d3c 100644
--- a/src/styles/media-grid.css
+++ b/src/styles/media-grid.css
@@ -694,3 +694,20 @@
   height: 100%;
   object-fit: cover;
 }
+
+@media (max-width: 48rem) {
+  .media-overlap-three {
+    display: grid;
+    gap: var(--gap);
+    min-height: 0;
+    overflow: visible;
+  }
+
+  .media-overlap-three > .media-item {
+    position: static;
+    width: 100%;
+    transform: none;
+  }
+}
+
+
diff --git a/src/styles/project-responsibilities.css b/src/styles/project-responsibilities.css
index 279548e..ba79369 100644
--- a/src/styles/project-responsibilities.css
+++ b/src/styles/project-responsibilities.css
@@ -205,14 +205,22 @@
 }
 
 @media (max-width: 48rem) {
+  .project-responsibilities,
+  .project-responsibilities--two,
+  .project-responsibilities--three {
+    grid-template-columns: minmax(0, 1fr);
+    gap: var(--gap);
+  }
+
   .project-responsibilities--mobile-scroll.project-responsibilities--three {
     display: grid;
     grid-auto-flow: column;
-    grid-auto-columns: minmax(17rem, min(78vw, 24rem));
+    grid-auto-columns: minmax(min(78vw, 17rem), min(82vw, 23rem));
     grid-template-columns: none;
     width: 100%;
     max-width: 100%;
     overflow-x: auto;
+    overflow-y: visible;
     overscroll-behavior-inline: contain;
     padding-bottom: 0.25rem;
     scroll-snap-type: x mandatory;
@@ -226,23 +234,42 @@
 
   .project-responsibilities--mobile-scroll.project-responsibilities--three > .responsibility-card {
     width: 100%;
-    min-width: 17rem;
-    max-width: 24rem;
+    min-width: 0;
+    max-width: none;
     grid-column: auto;
     scroll-snap-align: start;
   }
-}
 
-@media (max-width: 48rem) {
-  .project-responsibilities,
-  .project-responsibilities--two,
-  .project-responsibilities--three {
-    grid-template-columns: minmax(0, 1fr);
+  .project-responsibilities--contained-lists {
+    --responsibility-card-min-height: auto;
+    --responsibility-list-max-height: none;
+    --responsibility-list-bottom-space: 0;
   }
 
-  .project-responsibilities--contained-lists {
-    --responsibility-card-min-height: min(34rem, 78vh);
-    --responsibility-list-max-height: min(26rem, 58vh);
-    --responsibility-list-bottom-space: 4rem;
+  .project-responsibilities--contained-lists .responsibility-card {
+    grid-template-rows: auto;
+    min-height: 0;
+  }
+
+  .project-responsibilities--contained-lists .responsibility-card__list {
+    max-height: none;
+    padding-right: 0;
+    padding-bottom: 0;
+    overflow: visible;
+    scrollbar-gutter: auto;
+  }
+
+  .responsibility-card {
+    padding: clamp(1rem, 5vw, 1.35rem);
+    border-radius: var(--r-lg);
+    background: var(--white);
+    box-shadow: var(--surface-elevation-1);
+  }
+
+  .responsibility-card__title,
+  .project-responsibilities--semantic .responsibility-card__title {
+    font-size: var(--md);
   }
 }
+
+
diff --git a/src/styles/project.css b/src/styles/project.css
index 903a048..a032fd3 100644
--- a/src/styles/project.css
+++ b/src/styles/project.css
@@ -124,8 +124,68 @@
 }
 
 @media (max-width: 48rem) {
+  .project {
+    gap: var(--project-gap);
+    padding-block: var(--project-gap);
+  }
+
+  .project__header {
+    gap: var(--component-gap);
+  }
+
   .project__head,
   .project__head--text-only {
-    grid-template-columns: 1fr;
+    grid-template-columns: minmax(0, 1fr);
+    gap: var(--component-gap);
+  }
+
+  .project__head > .meta {
+    justify-self: center;
+    text-align: center;
+  }
+
+  .project__intro,
+  .project__head > div {
+    width: 100%;
+    max-width: 100%;
+    justify-items: center;
+  }
+
+  .project__intro > .title,
+  .project__head > div > .title {
+    max-width: min(100%, 13ch);
+    text-align: center;
+  }
+
+  .project__intro > p,
+  .project__head > div > p {
+    justify-self: start;
+    max-width: var(--text);
+    text-align: left;
+  }
+
+  .project__logo {
+    justify-self: center;
+  }
+
+  .project__logo img,
+  .project__logo canvas {
+    width: min(42vw, 10rem);
+  }
+
+  .project-section,
+  .block {
+    gap: var(--component-gap);
+    padding-block: var(--project-gap);
+  }
+
+  .project-section .project-section {
+    padding-block: var(--component-gap);
+  }
+
+  .project-skill-cloud__list {
+    max-width: 100%;
   }
 }
+
+
diff --git a/src/styles/proximity.css b/src/styles/proximity.css
index b9ab1db..466c8d4 100644
--- a/src/styles/proximity.css
+++ b/src/styles/proximity.css
@@ -1,13 +1,17 @@
 /* proximity */
 
 .proximity-control {
+  --proximity-control-size: clamp(1.75rem, 2.1vw, 2.2rem);
+  --proximity-control-icon: 0.92rem;
+  --proximity-control-inset: clamp(0.5rem, 1.2vw, 0.85rem);
+
   position: absolute;
-  z-index: 4;
+  z-index: 8;
 
   display: inline-grid;
   place-items: center;
 
-  width: clamp(2rem, 3vw, 2.5rem);
+  width: var(--proximity-control-size);
   aspect-ratio: 1;
   padding: 0;
 
@@ -29,12 +33,13 @@
   pointer-events: none;
   cursor: pointer;
   will-change: opacity, transform;
+  box-shadow: 0 0.35rem 1.2rem rgba(0, 0, 0, 0.08);
 }
 
 .proximity-control svg {
   display: block;
-  width: 1rem;
-  height: 1rem;
+  width: var(--proximity-control-icon);
+  height: var(--proximity-control-icon);
   color: currentColor;
 }
 
@@ -46,7 +51,9 @@
 }
 
 .proximity-control:focus-visible {
-  box-shadow: 0 0 0 0.18rem rgba(0, 0, 0, 0.18);
+  box-shadow:
+    0 0 0 0.18rem rgba(0, 0, 0, 0.18),
+    0 0.35rem 1.2rem rgba(0, 0, 0, 0.08);
 }
 
 .proximity-control.is-proximity-interactive {
@@ -59,18 +66,32 @@
 }
 
 .proximity-control--top-right {
-  top: var(--gap);
-  right: var(--gap);
+  top: var(--proximity-control-inset);
+  right: var(--proximity-control-inset);
 }
 
 .proximity-control--bottom-right {
-  right: var(--gap);
-  bottom: var(--gap);
+  right: var(--proximity-control-inset);
+  bottom: var(--proximity-control-inset);
 }
 
 .proximity-control--bottom-left {
-  bottom: var(--gap);
-  left: var(--gap);
+  bottom: var(--proximity-control-inset);
+  left: var(--proximity-control-inset);
+}
+
+.proximity-control--video,
+.proximity-control--video-link {
+  background: var(--black, #000);
+  color: var(--white, #fff);
+}
+
+.proximity-control--video:hover,
+.proximity-control--video:focus-visible,
+.proximity-control--video-link:hover,
+.proximity-control--video-link:focus-visible {
+  background: var(--white, #fff);
+  color: var(--black, #000);
 }
 
 .has-proximity-control {
@@ -83,12 +104,25 @@
 .embedded-demo,
 [data-animation],
 [data-visual-demo],
+[data-showcase-inline-video],
 pre,
 .code-block,
 .code-inspector {
   position: relative;
 }
 
+.media-item > .proximity-control,
+.media > .proximity-control,
+.embedded-demo > .proximity-control,
+[data-animation] > .proximity-control,
+[data-visual-demo] > .proximity-control,
+[data-showcase-inline-video] > .proximity-control,
+pre > .proximity-control,
+.code-block > .proximity-control,
+.code-inspector > .proximity-control {
+  position: absolute;
+}
+
 .media-slider__dots {
   opacity: 0;
   visibility: visible;
@@ -162,27 +196,39 @@ pre,
 }
 
 @media (max-width: 48rem) {
-  .proximity-control--top-right {
-    top: 0.75rem;
-    right: 0.75rem;
+  .proximity-control {
+    --proximity-control-size: 1.8rem;
+    --proximity-control-icon: 0.82rem;
+    --proximity-control-inset: 0.55rem;
   }
+}
 
-  .proximity-control--bottom-right {
-    right: 0.75rem;
-    bottom: 0.75rem;
+@media (hover: none) {
+  .proximity-control--open,
+  .proximity-control--fullscreen,
+  .proximity-control--video,
+  .proximity-control--video-link {
+    display: none;
   }
 
-  .proximity-control--bottom-left {
-    bottom: 0.75rem;
-    left: 0.75rem;
+  .media-slider__arrow--proximity {
+    display: none;
+  }
+
+  .media-slider__dots {
+    opacity: 1;
+    visibility: visible;
+    transform: translateY(0) scaleX(1) scaleY(1);
+    pointer-events: auto;
+  }
+
+  .media-caption--proximity {
+    opacity: 1;
+    visibility: visible;
+    transform: translateY(0) scaleX(1) scaleY(1);
+    pointer-events: auto;
   }
-}
 
-@media (hover: none) {
-  .proximity-control,
-  .media-slider__dots,
-  .media-slider__arrow--proximity,
-  .media-caption--proximity,
   .lightbox__close--proximity {
     opacity: 1;
     visibility: visible;
@@ -190,13 +236,23 @@ pre,
     pointer-events: auto;
   }
 
+  .proximity-control--copy {
+    --proximity-control-size: 1.8rem;
+    --proximity-control-icon: 0.82rem;
+
+    opacity: 1;
+    visibility: visible;
+    transform: translateY(0) scaleX(1) scaleY(1);
+    pointer-events: auto;
+  }
+
   .showcase-toc {
     --showcase-toc-link-width: auto;
     --showcase-toc-text-opacity: 1;
   }
 }
 
-@media (prefers-reduced-motion: reduce) {
+@media (prefers-reduced-motion: reduce) and (hover: hover) and (pointer: fine) {
   .proximity-control,
   .media-slider__dots,
   .media-slider__arrow--proximity,
diff --git a/src/styles/random-gallery.css b/src/styles/random-gallery.css
index 2a71f60..ad3bd30 100644
--- a/src/styles/random-gallery.css
+++ b/src/styles/random-gallery.css
@@ -140,7 +140,7 @@
 .random-gallery.is-enhanced.is-visible .random-gallery__item:nth-child(9) { transition-delay: 280ms; }
 .random-gallery.is-enhanced.is-visible .random-gallery__item:nth-child(n + 10) { transition-delay: 315ms; }
 
-@media (max-width: 760px) {
+@media (max-width: 48rem) {
   .random-gallery__bar {
     align-items: flex-start;
     flex-direction: column;
@@ -149,6 +149,11 @@
   .random-gallery__shuffle {
     width: 100%;
   }
+
+  .random-gallery__item {
+    border-radius: var(--r-lg);
+    box-shadow: var(--surface-elevation-1);
+  }
 }
 
 @media (prefers-reduced-motion: reduce) {
@@ -164,3 +169,4 @@
     transform: none;
   }
 }
+
diff --git a/src/styles/resume.css b/src/styles/resume.css
index c2b7b92..75d3705 100644
--- a/src/styles/resume.css
+++ b/src/styles/resume.css
@@ -28,10 +28,28 @@
   display: grid;
   gap: 0.7rem;
   max-width: var(--heading);
+  min-width: 0;
 }
 
 @media (max-width: 48rem) {
+  .resume-head {
+    text-align: center;
+  }
+
+  .resume-head > .title {
+    justify-self: center;
+    max-width: min(100%, 12ch);
+  }
+
   .resume-item {
-    grid-template-columns: 1fr;
+    grid-template-columns: minmax(0, 1fr);
+    gap: 0.9rem;
+    padding-block: var(--gap-lg);
+  }
+
+  .resume-item__header {
+    max-width: 100%;
   }
 }
+
+
diff --git a/src/styles/showcase-toc.css b/src/styles/showcase-toc.css
index ae11354..20ff765 100644
--- a/src/styles/showcase-toc.css
+++ b/src/styles/showcase-toc.css
@@ -72,12 +72,16 @@
 @media (max-width: 64rem) {
   .showcase-toc {
     top: auto;
-    right: clamp(10px, 3vw, 20px);
+    right: var(--page-x);
     bottom: max(10px, env(safe-area-inset-bottom));
-    left: clamp(10px, 3vw, 20px);
+    left: var(--page-x);
     display: block;
     transform: none;
     overflow: hidden;
+    border: var(--line);
+    border-radius: 999px;
+    background: rgba(255, 255, 255, 0.92);
+    box-shadow: var(--surface-elevation-2);
   }
 
   .showcase-toc__list {
@@ -85,7 +89,8 @@
     gap: 0.45rem;
     max-width: 100%;
     overflow-x: auto;
-    padding-block: 0.25rem;
+    overflow-y: hidden;
+    padding: 0.42rem;
     scrollbar-width: none;
     -webkit-overflow-scrolling: touch;
   }
@@ -99,23 +104,29 @@
   .showcase-toc:focus-within .showcase-toc__link {
     flex: 0 0 auto;
     width: auto;
-    max-width: min(12rem, 72vw);
-    min-height: 1.6rem;
-    padding: 0.3rem 0;
+    min-height: 2rem;
+    max-width: min(13rem, 72vw);
+    padding: 0.38rem 0.65rem 0.42rem;
+    border-radius: 999px;
+  }
+
+  .showcase-toc__link::before {
+    flex-basis: 0.72rem;
+    width: 0.72rem;
   }
 
   .showcase-toc__text {
-    width: 0;
-    max-width: 0;
+    width: auto;
+    max-width: min(11rem, 58vw);
     overflow: hidden;
     opacity: 1;
+    text-overflow: ellipsis;
   }
 
-  .showcase-toc__link.is-active .showcase-toc__text,
-  .showcase-toc__link:focus-visible .showcase-toc__text,
-  .showcase-toc:hover .showcase-toc__text,
-  .showcase-toc:focus-within .showcase-toc__text {
-    width: auto;
-    max-width: min(10rem, 58vw);
+  .showcase-toc__link.is-active {
+    background: var(--black);
+    color: var(--white);
   }
 }
+
+
diff --git a/src/styles/site-header.css b/src/styles/site-header.css
index 7cd6724..8f02895 100644
--- a/src/styles/site-header.css
+++ b/src/styles/site-header.css
@@ -192,23 +192,39 @@
     --site-header-chip-gap: clamp(0.48rem, 2.4vw, 0.74rem);
     --site-header-chip-font: clamp(0.82rem, 3.4svw, 1.02rem);
 
-    justify-content: flex-end;
+    justify-content: stretch;
     padding-block-start: max(0.72rem, env(safe-area-inset-top));
     padding-block-end: 0.62rem;
+    padding-inline: 0;
   }
 
   .site-header__island {
-    justify-items: end;
-    inline-size: fit-content;
-    max-inline-size: calc(100svw - var(--site-header-page-x) - var(--site-header-page-x-right));
-    margin-inline-start: auto;
-    margin-inline-end: 0;
+    justify-items: stretch;
+    inline-size: 100%;
+    max-inline-size: 100%;
+    margin: 0;
     transform-origin: top right;
   }
 
   .site-header__chips {
-    justify-content: flex-end;
-    max-inline-size: calc(100svw - var(--site-header-page-x) - var(--site-header-page-x-right));
+    justify-self: stretch;
+    justify-content: flex-start;
+    inline-size: 100%;
+    max-inline-size: 100%;
+    min-inline-size: 0;
+    overflow-x: auto;
+    overflow-y: visible;
+    overscroll-behavior-inline: contain;
+    padding-inline-start: var(--site-header-page-x);
+    padding-inline-end: var(--site-header-page-x-right);
+    padding-block: 0.2rem;
+    scroll-padding-inline: var(--site-header-page-x);
+    scrollbar-width: none;
+    -webkit-overflow-scrolling: touch;
+  }
+
+  .site-header__chips::-webkit-scrollbar {
+    display: none;
   }
 
   .site-header__chip {
@@ -224,10 +240,15 @@
     inline-size: 4.12rem;
     block-size: 4.12rem;
     min-block-size: 4.12rem;
+    margin-inline-end: var(--site-header-page-x-right);
     padding: 0.36rem;
     touch-action: manipulation;
   }
 
+  .site-header__island.is-collapsed {
+    justify-items: end;
+  }
+
   .site-header__island.is-collapsed .site-header__trigger {
     opacity: 1;
     visibility: visible;
@@ -249,7 +270,7 @@
 
   .site-header__menu {
     inset-block-start: calc(100% + clamp(0.54rem, 2.8svw, 0.82rem));
-    inset-inline-end: 0;
+    inset-inline-end: var(--site-header-page-x-right);
     inset-inline-start: auto;
     justify-items: end;
     gap: clamp(0.5rem, 2.6svw, 0.74rem);
@@ -267,3 +288,4 @@
   }
 }
 /* component site-header:end */
+
diff --git a/src/styles/typography.css b/src/styles/typography.css
index 80e716b..c56737b 100644
--- a/src/styles/typography.css
+++ b/src/styles/typography.css
@@ -7,11 +7,15 @@ p {
 }
 
 .title {
+  max-width: 100%;
+  min-width: 0;
   font-weight: 700;
   line-height: 0.95;
   letter-spacing: -0.04em;
   text-transform: lowercase;
   text-wrap: balance;
+  overflow-wrap: anywhere;
+  word-break: normal;
 }
 
 .title--display,
@@ -32,9 +36,11 @@ p {
   display: flex;
   flex-wrap: wrap;
   gap: 0.35rem;
+  min-width: 0;
 }
 
 .chip {
+  min-width: 0;
   padding: 0.26rem 0.5rem 0.3rem;
   border: var(--line);
   border-radius: 999px;
@@ -51,11 +57,41 @@ p {
   display: grid;
   gap: 0.35rem;
   max-width: var(--text);
+  min-width: 0;
   padding: 0;
   list-style: none;
 }
 
 .text-list li {
+  min-width: 0;
   font-size: var(--md);
   line-height: 1.25;
 }
+
+@media (max-width: 48rem) {
+  p,
+  .text-list li {
+    line-height: 1.22;
+  }
+
+  .title--display,
+  .title--xl {
+    line-height: 0.96;
+    letter-spacing: -0.052em;
+  }
+
+  .title--lg {
+    line-height: 1;
+    letter-spacing: -0.042em;
+  }
+
+  .chips {
+    gap: 0.42rem;
+  }
+
+  .chip {
+    padding: 0.28rem 0.58rem 0.32rem;
+  }
+}
+
+
