export function getPlaylistFilterData() {
  return {
    source: "legacy-app",
    note: "Data extraction boundary. The visual implementation is preserved in legacy-app.js until semantic extraction is complete.",
  };
}
