{
  "name": "vanila-prototype",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run media:video:build && npm run media:build && vite",
    "typecheck": "tsc -p tsconfig.json",
    "build": "npm run media:video:build && npm run media:build && npm run typecheck && vite build",
    "media:build": "node tools/build-responsive-media.mjs",
    "media:video": "node tools/build-video-media.mjs",
    "media:video:build": "node tools/build-video-media.mjs --build",
    "preview": "vite preview",
    "test": "npm run media:video:build && npm run media:build && node --test && node tools/check-data-integrity.ts",
    "test:media": "npm run media:video:build && npm run media:build && node --test \"test/media-tools/*.test.mjs\" && node tools/check-data-integrity.ts",
    "test:e2e": "node tools/smoke-site.mjs",
    "verify": "npm run typecheck && npm test && npm run build && npm run test:e2e"
  },
  "devDependencies": {
    "playwright": "^1.61.1",
    "sharp": "^0.34.5",
    "typescript": "^7.0.2",
    "vite": "^8.0.10"
  },
  "dependencies": {
    "@fontsource-variable/inter": "^5.3.0",
    "@fontsource-variable/rubik": "5.3.0",
    "gsap": "^3.15.0",
    "three": "^0.184.0"
  },
  "engines": {
    "node": "24.x"
  }
}
