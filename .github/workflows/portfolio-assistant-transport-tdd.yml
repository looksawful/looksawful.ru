name: Portfolio Assistant Transport TDD

on:
  push:
    branches:
      - contact-hub-frontend-current
    paths:
      - .github/workflows/portfolio-assistant-transport-tdd.yml
      - test/portfolio-assistant-transport-contract.test.mjs
      - src/features/portfolio-pet/assistant-transport.ts
      - src/features/portfolio-pet/portfolio-chat-service.ts

permissions:
  contents: read

concurrency:
  group: portfolio-assistant-transport-tdd-${{ github.ref }}
  cancel-in-progress: true

jobs:
  contract:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 1
          persist-credentials: false

      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: npm

      - run: npm ci

      - name: Focused assistant transport contract
        run: node --test test/portfolio-assistant-transport-contract.test.mjs

      - name: Typecheck
        run: npm run typecheck
