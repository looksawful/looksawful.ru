# Agent Context

These files are short, project-specific context for agents that start without a conversation. They summarize the canonical documents; they are not a second source of executable truth.

If a summary conflicts with code, tests, workflows, or the canonical document named in it, re-check the stronger source and update this summary only after the architecture decision is clear.
